import React from 'react';
import ReactDOM from 'react-dom/client';
import PontRegiszter from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <PontRegiszter />
  </React.StrictMode>
);
